apiVersion: apps/v1
kind: Deployment
metadata:
  name: emissions-elt-demo
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      bb: web
  template:
    metadata:
      labels:
        bb: web
    spec:
      containers:
        - name: eed-app
          image: us-west1-docker.pkg.dev/emissions-elt-demo/elt-demo-docker-repo/app
